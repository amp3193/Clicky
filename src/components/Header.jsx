import * as React from 'react'

function Header() {
    return (
        <header className="header">
        <h1>Pet all the Cats!</h1>
        <h3>Click each cat to pet them, but pet each only once because a cat made the rules.</h3>
        </header>
    );

}

export default Header;